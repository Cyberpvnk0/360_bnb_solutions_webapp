import { AnalyzingScreen } from "@/components/analyze/analyzing-screen";

/** The result's own layout with the property named and the work shown
 *  as working — see components/analyze/analyzing-screen. */
export default function AnalyzeResultLoading() {
  return <AnalyzingScreen />;
}
